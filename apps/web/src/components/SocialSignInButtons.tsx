'use client';

/**
 * Appwrite email/password auth is the active production path.
 * Social OAuth buttons are intentionally hidden until Appwrite OAuth providers
 * and callback routes are configured for this project.
 */
export function SocialSignInButtons(_props: { callbackUrl: string }) {
  return null;
}
